{
  "name": "tab-garden",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc && cp src/manifest.json src/popup.html src/popup.css src/board.html src/board.css src/options.html src/options.css dist/ && node scripts/inject-env.mjs",
    "typecheck": "tsc --noEmit",
    "test": "npm run build && node --test tests/*.test.js"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.326",
    "typescript": "^5.8.3"
  }
}
